{
	"name": "@halo-lab/jessie",
	"version": "0.1.0",
	"description": "Utility for defining dimensions of elements based on the alleged grid",
	"scripts": {
		"prepublishOnly": "npm run build",
		"start": "parcel ./test/index.html",
		"prebuild": "rm -rf docs",
		"build": "parcel build ./test/index.html"
	},
	"dependencies": {
		"sass": "^1.47.0"
	},
	"targets": {
		"default": {
			"distDir": "docs",
			"publicUrl": "/jessie"
		}
	},
	"publishConfig": {
		"access": "public"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/Halo-Lab/jessie"
	},
	"license": "ISC",
	"devDependencies": {
		"@parcel/transformer-sass": "^2.1.1",
		"parcel": "^2.1.1"
	}
}
